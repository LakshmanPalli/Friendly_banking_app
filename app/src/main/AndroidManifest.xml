<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bankapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bank_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.bankapp.SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.bankapp.MainActivity"
            />
        <activity
            android:name="com.example.bankapp.AccHolders"
            android:label="Accounts" />
        <activity
            android:name="com.example.bankapp.TransHistoryList"
            android:label="Transactions" />
        <activity
            android:name="com.example.bankapp.SendtoUser"
            android:label="Select Receiver" />
        <activity
            android:name="com.example.bankapp.AccountInfo"
            android:label="Account Information" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>